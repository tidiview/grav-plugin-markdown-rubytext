name: Markdown RubyText
version: 1.1.0
description: "Adds ability to output ruby tag text in Markdown"
icon:  language
author:
  name: François VIDIT
  email: francois.vidit@laposte.net
  url: http://francois-vidit.com
license: MIT

form:
  validation: strict
  fields:
    enabled:
      type: toggle
      label: Plugin status
      highlight: 1
      default: 0
      options:
        1: Enabled
        0: Disabled
      validate:
        type: bool
